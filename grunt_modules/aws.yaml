aws: grunt.file.readJSON("aws-credentials.json")
s3:
  options:
    accessKeyId: "<%= aws.accessKeyId %>",
    secretAccessKey: "<%= aws.secretAccessKey %>",
    bucket: "jazzberrygames.com"
  deploy:
    cwd: "dist/"
    src: "**"
